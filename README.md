This project is incomplete 
and there is an artisan command to run all seeders
the command is php artisan execute-all-seeders-command
most controllers are incomplete but The most interesting parts is app service provider , redis controller , maybe web.php routes combining method , ajax usage on blades
so it's 95% js and blade ! 
